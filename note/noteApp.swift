import SwiftUI

@main
struct NoteApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView() // ContentView is directly specified as the content of the WindowGroup
        }
    }
}
